# ms_api

```bash
git config --global http.proxy http://127.0.0.1:7890  #设置代理为系统代理
git config --global https.proxy http://127.0.0.1:7890 #设置代理为系统代理
git config --global http.sslVerify false 解除ssl验证防止SSL_read: Connection was reset, errno 10054
```
